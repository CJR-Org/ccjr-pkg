CCJR (CJR Compiler) distributed as a moduland package, requires sudo privileges.  

# Install
`pcjr install ccjr`  
  
A packages.json file and modules folder will remain after the install and can be removed if not being used.